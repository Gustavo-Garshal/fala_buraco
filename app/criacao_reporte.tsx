import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function criar_reporte() {
    return (
        <SafeAreaView>
            <View>
                <Text>
                    Criando um reposrte do problema
                </Text>
            </View>
        </SafeAreaView>
    );
}